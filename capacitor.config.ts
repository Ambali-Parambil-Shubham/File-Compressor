import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.mosszip.studio',
  appName: 'MossZip Studio',
  webDir: 'frontend/dist'
};

export default config;
